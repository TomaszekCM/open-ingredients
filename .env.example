APP_HOST=0.0.0.0
APP_PORT=8000

POSTGRES_DB=openingredients
POSTGRES_USER=openingredients
POSTGRES_PASSWORD=openingredients

# Local dev JWT secret (change this in real environments)
JWT_SECRET_KEY=replace-with-long-random-secret
